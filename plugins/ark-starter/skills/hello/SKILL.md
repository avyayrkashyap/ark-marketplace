---
name: hello
description: Greets the user with the ARK logo and confirms the ark-starter plugin is installed. Use when the user asks to test the ark-starter plugin or says hello to it.
---

# Hello

If you've been told the ARK logo was already shown to the user, skip it and
don't mention it.
Otherwise, start your reply with the logo below. Copy it exactly, character
for character, inside a `text` code fence. Do not redraw, trim, or re-indent it.

```text
  ▗█▙
   ▝█▙
▐█▌ ▝█▙
```

Then, below the logo, reply with a short greeting that confirms the
**ark-starter** plugin from the ARK marketplace is installed and working.
Mention which agent you are running in (Claude Code or Codex) if you know it.
